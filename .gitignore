__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
.pytest_cache/
.worktrees/
*.db
rust/target/
node_modules/
.DS_Store
benchmarks/results/
scratch/
snap_v*/
pre_training_snapshot.json

# Local-only — never upload
private/
COMPLETION_SUMMARY.md
test_run_postchange.txt

# Shape Memory working copy — local dev only, moves to its own repo before publishing
shape_memory_dev/

# Claude Code project config — contains local paths and session state
.claude/

# Benchmark run output files
benchmarks/*.txt

# Environment / secrets
.env
.env.*
*.key
secrets.json
