services:
  redis-stack:
    image: redis/redis-stack-server:latest
    container_name: lattice-redis-stack
    ports:
      - "6382:6379"
    volumes:
      - lattice-redis-stack-data:/data
    restart: unless-stopped

volumes:
  lattice-redis-stack-data:
