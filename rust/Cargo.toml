[package]
name = "latticememory_kernel"
version = "0.1.0"
edition = "2021"
description = "High-performance Rust/WASM snapping kernel for the E8 lattice."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.92"

[profile.release]
# Optimize WASM size
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
